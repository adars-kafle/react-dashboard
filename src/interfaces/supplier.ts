export type Supplier = {
  id: number;
  name: string;
  address: string;
  email: string;
  phone: string;
};

export type SupplierFormInputs = {
  name: string;
  address: string;
  email: string;
  phone: string;
};
